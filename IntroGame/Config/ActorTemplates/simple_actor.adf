ActorFactory.InitializeActor(Actor.Create())

; ActorFactory.InitializeActor() starts an implied Using() 
; block until the end of the file.  All functions / properties
; are called on the new object until the end of the file.

Color = Color(1.0, 0.0, 1.0, 0.5)
Size = Vector2(5.0, 5.0)